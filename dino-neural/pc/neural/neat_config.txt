# Configuração do NEAT para o Dino Neural.
# 4 entradas (dist, vel, topo_obstaculo, base_obstaculo) -> 2 saídas (pular, agachar).
# As posições vertical (topo/base) permitem distinguir cacto (pular) de
# pterodáctilo baixo (agachar). A topologia começa mínima e cresce ao evoluir.

[NEAT]
fitness_criterion     = max
fitness_threshold     = 3000
no_fitness_termination = False
pop_size              = 120
reset_on_extinction   = True

[DefaultGenome]
# nós
num_inputs              = 4
num_hidden              = 0
num_outputs             = 2
initial_connection      = full_direct
feed_forward            = True

# ativação
activation_default      = tanh
activation_options      = tanh
activation_mutate_rate  = 0.0
aggregation_default     = sum
aggregation_options     = sum
aggregation_mutate_rate = 0.0

# compatibilidade (especiação)
compatibility_disjoint_coefficient = 1.0
compatibility_weight_coefficient   = 0.5

# probabilidades de mutação estrutural
conn_add_prob           = 0.6
conn_delete_prob        = 0.2
node_add_prob           = 0.3
node_delete_prob        = 0.2

# bias
bias_init_mean          = 0.0
bias_init_stdev         = 1.0
bias_max_value          = 30.0
bias_min_value          = -30.0
bias_mutate_power       = 0.5
bias_mutate_rate        = 0.7
bias_replace_rate       = 0.1

# conexões habilitadas
enabled_default         = True
enabled_mutate_rate     = 0.01

# resposta do nó (mantida fixa)
response_init_mean      = 1.0
response_init_stdev     = 0.0
response_max_value      = 30.0
response_min_value      = -30.0
response_mutate_power   = 0.0
response_mutate_rate    = 0.0
response_replace_rate   = 0.0

# pesos
weight_init_mean        = 0.0
weight_init_stdev       = 1.0
weight_max_value        = 30
weight_min_value        = -30
weight_mutate_power     = 0.5
weight_mutate_rate      = 0.8
weight_replace_rate     = 0.1

[DefaultSpeciesSet]
compatibility_threshold = 3.0

[DefaultStagnation]
species_fitness_func = max
max_stagnation       = 20
species_elitism      = 2

[DefaultReproduction]
elitism            = 2
survival_threshold = 0.2
